<h1>User#index</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to "Add as friend", send_invitation(user.id, current_user.id), remote: true %>
    </p>
  <% end %>
</div>
