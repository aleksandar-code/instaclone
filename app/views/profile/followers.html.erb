<h2>Follower list of <%= @user.email %></h2>

<% @user.followers.each do |user| %>
    <%= link_to user.email, profile_path(user) %>
    <% if current_user.following?(user) %>
      <%= button_to "Unfollow", unfollow_path(user) %>
    <% end %>
<% end %>
